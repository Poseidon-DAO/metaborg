import type { NextPage } from "next";

import { Header } from "../components";

const Home: NextPage = () => {
  return (
    <div className="bg-secondary">
      <Header />
    </div>
  );
};

export default Home;
